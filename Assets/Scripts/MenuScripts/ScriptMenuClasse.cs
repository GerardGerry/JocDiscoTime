using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScriptMenuClasse : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}


/*
  The canvas:
 - Root object for all UI elements, like a canvas where you can stick UI elements on it.

  Canvas scaling:
- Constant Pixel Size:
- Scale with Sceen Size (recommended): You have to decide the size difference.



 Score nota: Free Spect, mirar que no es descuadri del tot el menu al escalar (perfecte es impossible)
 

 Nota escalable menus -> Model View Controller: Model -> Valor punts de vida View -> El que mostres Controller -> El que et permet cambiar-ho


 UI Juice 

 Animations good :) 


 Canvas Group -> To work with multiple Canvas for cool fx/animations

InGameMenuController -> SetPaused / UnSetPaused methods, to hide things (Example, HUD)

 */